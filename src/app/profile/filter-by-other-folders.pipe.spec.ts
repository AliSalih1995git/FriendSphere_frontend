import { FilterByOtherFoldersPipe } from './filter-by-other-folders.pipe';

describe('FilterByOtherFoldersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByOtherFoldersPipe();
    expect(pipe).toBeTruthy();
  });
});
